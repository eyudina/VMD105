extends _layout.pug

prepend navigation
    -var current_page = 'print'

block content
    section.content
        .wrapper
            iframe(src="./assets/documents/print.pdf#toolbar=0" width="100%" height="500px")
            article
                h1 InDesign Project
                p Here is my coursework portfolio. The PDF file on the left was created in InDesign.
